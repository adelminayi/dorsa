# dorsa
Throttling practice

# Steps to run code
```

python3 -m venv venv
. venv/bin/activate

pip install -r requirements.txt

python3 manage.py runsever
```

login via:
```
localhost:8000/api/token/
user:admin
pass:admin
```